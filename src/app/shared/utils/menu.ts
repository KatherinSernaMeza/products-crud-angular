import { MenuItem } from '../../Core/models/menu.interface';

// Declaración de un array de MenuItem[]
let menuItems: MenuItem[] = [
  { label: 'Inicio', route: 'home'! || '' },
  { label: 'Productos', route: '/products'! || '' },
  // Otros elementos del menú...
];
